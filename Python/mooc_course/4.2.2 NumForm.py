# 4.2.2 NumForm.py

a = pow(2,100)
print(a)
# pow(x,y)
# Calculate x power y

# binary system starts with 0b / 0B
# 0b010 / -0B101

# octonary system starts with 0o / 0O
# 0o123 / -0O456

# hexadecimal system starts with 0x / 0X
# 0x9a / -0X89

# 0.1 + 0.2 == 0.3
# >>> False

# round(x,d) x = rounding, d = decimal
# round(0.1 + 0.2, 1) == 0.3
# >>> True 

# Scientific notation
# <a>e<b> = a * 10^b
# 4.3e-3 = 0.0043

# Complex number
# z = 1.23e-4 + 5.6e+89j
# z.real
# >>> 1.23e-4
# z.imag
# >>> 5.6e+89